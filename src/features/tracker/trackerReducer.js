const initialState = {
  isTrackersLoading: false,
  trackers: [],
};

const trackerReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default trackerReducer;
